import agent_u_partial


"""Creates V Partial Training Data for current set 
(these files are not submitted due to their size, they are only needed to 
re-run visualization or to re-train v partial, if create_new env is run locally
it will automatically be generated)"""
agent_u_partial.generate_train_data()